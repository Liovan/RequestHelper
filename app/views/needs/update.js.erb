$("#NeedForm").modal("hide");
$(".need_<%= @need.id %>").fadeOut(600);
$(".need_<%= @need.id %>").fadeIn(500);
$(".need_<%= @need.id %> .name").text("<%= @need.name %>");
$(".need_<%= @need.id %> .type").text("<%=requirement_types.key(@need.input_type)  %>");
$("#alert_respond").text('').fadeOut(300).fadeIn(100);
$("#alert_respond").prepend("<%= j render partial: "layouts/flash_message" %>");
